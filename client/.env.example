# Copy to .env.local for local development (pnpm dev outside Docker).
# Inside docker compose these are set by docker-compose.yaml instead.

# Used by Server Components (page.tsx) — runs on the Next server.
API_URL=http://localhost:4200

# Used by Client Components (TaskList.tsx) — runs in the browser.
NEXT_PUBLIC_API_URL=http://localhost:4200
